# ipset文件补全模块 (ipset)

本 Magisk/KernelSU 模块旨在为 Android 设备补全 `ipset` 可执行文件及相关依赖库（`libmnl.so`），使支持该功能的系统能够正常使用 `ipset` 命令。

## ⚠️ 重要前提

**本模块仅提供用户态工具文件，不提供内核级支持。**
要成功使用 `ipset` 命令，您的设备内核必须已经编译并启用了 `ipset` 功能。如果内核不支持，即使安装了此模块，`ipset` 命令依然无法正常工作。

## 🔍 如何检查内核是否支持 ipset？

在安装本模块前，请先通过 Root 权限确认您的内核配置。在终端中执行以下命令：

```bash
zcat /proc/config.gz | grep -i IP_SET
```

- **支持**：如果输出包含 `CONFIG_IP_SET=y`（或 `CONFIG_IP_SET=m`），说明内核已支持，您可以放心安装本模块。
- **不支持**：如果没有输出，或者显示 `# CONFIG_IP_SET is not set`，说明当前内核不支持。您需要自行刷入支持 `ipset` 的第三方内核，否则安装本模块没有意义。

## 📦 包含的文件

- `system/bin/ipset`：ipset 核心可执行文件
- `system/lib64/libmnl.so`：ipset 运行所需的底层网络链接库

## 🛠️ 安装方法

**kernelSU需要安装元模块**

1. 前往本仓库的 [Releases](../../releases) 页面下载最新的 `ipset-vX.X.X.zip` 文件。
2. 打开 KernelSU 或 Magisk Manager 应用。
3. 选择“本地安装”或点击右下角的 `+` 号，选中刚才下载的 ZIP 文件进行安装。
4. 安装过程中，脚本会自动检测内核是否支持 ipset。如果不支持，安装将被自动中止。
5. 安装成功后，请**重启设备**以使挂载生效。

## 📝 免责声明

- 请在操作前确保您了解相关风险，因刷入模块导致的任何问题由用户自行承担。
- 建议在执行涉及防火墙规则的操作前做好备份。
